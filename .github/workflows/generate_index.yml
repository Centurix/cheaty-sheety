name: CI
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the cheat sheet index.json
        run: python -c 'print("hello mum")'
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build
